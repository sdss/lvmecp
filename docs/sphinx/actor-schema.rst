.. _actor-schema:

Actor schema
============

In additional to these keywords, the actor also supports the default keywords automatically added by `CLU <https://clu.readthedocs.io/en/latest/index.html>`__. Actors that subclass from `.lvmecp` may implement additional keywords.

.. jsonschema:: ../../python/lvmecp/etc/schema.json

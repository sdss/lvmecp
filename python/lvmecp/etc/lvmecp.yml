#A dictionary of controller name to plc controller connection parameters

devices:
    controllers:
        simulator:
            testcontroller:
                host: 127.0.0.1
                port: 502
        #plc_controllers:
        #    plc1:
        #        host:
        #        port:

timeouts:
    controller_connect: 1

actor:
    name: lvmecp
    host: localhost
    port: